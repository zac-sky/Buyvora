# 验证记录

日期：2026-09-07。记录实际执行结果，不把未来计划计作已完成。

## 本地验证

- Windows 本地自动化测试：104 项通过，覆盖商品搜索、模型协议、工具参数、会话隔离、并发取消、错误处理和调用限制。
- `python scripts/smoke_agent.py`：通过。使用独立的真实 HTTP 服务进程和本地协议测试服务执行两轮购物请求；预算从 300 元改到 200 元，商品数从 2 变 1。
- 依赖当前出现两条上游弃用提示（Starlette TestClient/httpx 和 AnyIO 的旧别名），不影响上述测试。后续升级相关依赖时需复测。

## 尚未证明

- 真实模型联调：当前无模型配置，尚未执行 `scripts/smoke_agent.py --live`。
- 真实模型的自然语言理解、推荐质量和抗错误能力：需要真实模型和评测集。
- 会话重启恢复、长期偏好、价格规则、订单、前端与部署：仍在后续路线内。向量检索代码进展见下方第 4 阶段记录。

本地协议测试只证明应用能按协议调用模型接口和执行工具，不证明真实模型会做出正确决策。

## 第 4 阶段验证（2026-09-08）

- 本地自动化测试：155 项通过。增加向量数值与维度、批次索引映射、缓存重建、并发首次索引、重排序顺序、可见降级和知识来源测试。
- HTTP 协议脚本：两轮 Agent 流程、增强商品筛选和知识来源均通过。模式为 `scripted-protocol-fixture`，没有使用真实模型。
- 检索评测：17 条样本，12 条正例与 5 条无结果样本。关键词 Recall@3 为 0.25，本地降级为 0.9167；MRR@3 为 0.875；硬条件违规为 0。完整样本结果保存在 `eval/baseline-local.json`。
- 第 3 阶段提交 `3746a52` 的 GitHub CI 已确认全部成功：Linux Python 3.11、3.12 和 Windows Python 3.13，每个环境 104 项测试及两轮协议验证通过。记录链接：https://github.com/zac-sky/Buyvora/actions/runs/34106165027 。
- 真实聊天、embedding、reranker 的效果仍未验证；本阶段测试和本地样本不能替代这些验收。
