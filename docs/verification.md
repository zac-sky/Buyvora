# 验证记录

日期：2026-09-07。记录实际执行结果，不把未来计划计作已完成。

## 本地验证

- Windows 本地自动化测试：104 项通过，覆盖商品搜索、模型协议、工具参数、会话隔离、并发取消、错误处理和调用限制。
- `python scripts/smoke_agent.py`：通过。使用独立的真实 HTTP 服务进程和本地协议测试服务执行两轮购物请求；预算从 300 元改到 200 元，商品数从 2 变 1。
- 依赖当前出现两条上游弃用提示（Starlette TestClient/httpx 和 AnyIO 的旧别名），不影响上述测试。后续升级相关依赖时需复测。

## 尚未证明

- 真实模型联调：当前无模型配置，尚未执行 `scripts/smoke_agent.py --live`。
- 真实模型的自然语言理解、推荐质量和抗错误能力：需要真实模型和评测集。
- 会话重启恢复、长期偏好、向量检索、价格规则、订单、前端与部署：仍在后续路线内。

本地协议测试只证明应用能按协议调用模型接口和执行工具，不证明真实模型会做出正确决策。
