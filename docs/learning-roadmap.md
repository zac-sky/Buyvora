# Buyvora 学习与开发路线

这份路线把完整电商 Agent 拆成可以独立理解、独立测试、独立提交的小阶段。

## 阶段 0：工程基础（已完成）

学习 Python 项目结构、FastAPI、虚拟环境、自动化测试，以及 Git 的基本提交流程。

## 阶段 1：购物对话接口（已完成）

增加会话请求模型和 `POST /commerce/chat`，先用确定性的回复验证前后端数据流。学习 API schema、请求校验和错误处理。

## 阶段 2：商品领域模型（已完成）

建立商品、SKU、金额和库存等基础类型，并用内存商品仓储保存种子数据。学习领域模型与接口分离。

## 阶段 3：第一个可用 Agent

接入 OpenAI 兼容模型，增加商品搜索工具，让 Agent 能理解购物需求并调用搜索能力。学习提示词、工具调用和环境变量管理。

## 阶段 4：检索与推荐

从关键词搜索逐步升级到向量检索、重排序和结构化过滤。学习 RAG、召回质量和评测集设计。

## 阶段 5：记忆与价格

增加用户偏好记忆、汇率、运费和税费计算。学习状态管理、持久化和业务规则测试。

## 阶段 6：订单与安全边界

增加创建、查询和取消订单，并为写操作增加确认、幂等和权限边界。学习 Agent 不应擅自执行高风险操作。

## 阶段 7：可视化与部署

增加 React 对话界面、事件流、Docker 和基础观测能力，最终形成可展示的简历项目。

每个阶段都遵循：先理解概念 → 写最小实现 → 写测试 → 本地验证 → 提交 Git → 推送 GitHub。

## 最终交付验收

完整目标持续推进，前三阶段完成不代表整个 Agent 已完成。

- [x] 工程骨架、健康检查、锁定依赖、GitHub 仓库关联。
- [x] 购物对话请求校验与接口测试（目前固定回复）。
- [x] 商品与 SKU 模型、演示目录、预算与库存联合筛选。
- [ ] 真实模型适配、工具调用循环、商品推荐与多轮对话；配置缺失或失败需明确反馈。
- [ ] 向量召回、重排序、知识检索、降级策略和检索评测数据。
- [ ] 持久化对话与用户偏好；可测试的汇率、运费、税费计算。
- [ ] 演示订单创建、查询、取消，确认机制、幂等与用户隔离。
- [ ] React 对话界面、商品卡片和工具执行事件展示。
- [ ] Docker 部署、持续集成、端到端验证和基础日志/追踪。
- [ ] 每阶段独立提交与 GitHub 推送验证，配套组件学习笔记。
- [ ] 演示脚本、架构说明、设计取舍和基于已验证能力的简历介绍。

参考 `E:/globex-agent` 的功能方向和分层思路，Buyvora 的代码、演示数据和学习文档逐步独立实现。参考项目中的复杂组件需要在对应问题出现时解释引入原因。
